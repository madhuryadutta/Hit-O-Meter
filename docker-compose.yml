version: "3.4"
services:
  webserver:
    container_name: webserver
    build:
      context: .
      dockerfile: Dockerfile
    user: "1001:1001"
    # volumes:
    #   - ./:/var/www/html
    ports:
      - 8001:80